Server:
  port: 8001


#MyBatis的配置
mybatis:
  # api模块的全路径
  type-aliases-package: henu.soft.pojo

  mapper-locations: classpath:mybatis/mapper/*.xml
  # mybatis配置文件
  config-location: classpath:mybatis/mybatis-config.xml


# spring的配置
spring:
  application:
    name: springcloud-provider-dept
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/springcloud_dept?useUnicode=true&characterEncoding=utf-8
    username: root
    password: 720720
